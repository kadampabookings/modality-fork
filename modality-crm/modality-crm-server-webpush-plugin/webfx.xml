<?xml version="1.0" encoding="UTF-8"?>
<project>

    <description>KBS-side glue that adapts the generic webfx-stack-webpush-server module
        to KBS's domain: provides a `WebPushSubscriptionStore` implementation that updates
        the modality `PushSubscription` entity via the standard EntityStore/UpdateStore
        pipeline. Pulled in by kbs-server-application as a runtime plugin.</description>

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>
    </dependencies>

    <providers>
        <provider interface="dev.webfx.stack.webpush.spi.WebPushSubscriptionStore">one.modality.crm.server.webpush.ModalityWebPushSubscriptionStore</provider>
    </providers>

</project>
