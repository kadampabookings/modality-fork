<?xml version="1.0" encoding="UTF-8"?>
<!-- File managed by WebFX (DO NOT EDIT MANUALLY) -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>one.modality</groupId>
        <artifactId>modality-crm</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>modality-crm-server-webpush-plugin</artifactId>

    <description>KBS-side glue that adapts the generic webfx-stack-webpush-server module
        to KBS's domain. Provides:
        - a `WebPushSubscriptionStore` impl that stores and queries push subscriptions
          and their recipient links via Modality's `PushSubscription` /
          `PushSubscriptionRecipient` entities;
        - the `ModalityWebPushTarget` type (with its SerialCodec) used as the
          `target` payload of the generic `SendPushNotification` BO operation —
          carries the FK(s) that scope the recipient set (event / document /
          organization).
        Pulled in by kbs-server-application as a runtime plugin.</description>

    <dependencies>

        <dependency>
            <groupId>one.modality</groupId>
            <artifactId>modality-base-shared-entities</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-ast</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-async</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-console</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-com-serial</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-db-submit</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-orm-datasourcemodel-service</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-orm-entity</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-webpush-server</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

    </dependencies>

</project>