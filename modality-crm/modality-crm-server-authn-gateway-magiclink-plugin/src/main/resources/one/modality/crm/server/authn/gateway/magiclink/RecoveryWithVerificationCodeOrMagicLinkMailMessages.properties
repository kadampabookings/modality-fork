# English (default / fallback) dictionary for the password-recovery magic-link email.
# Out-of-body strings only — the HTML body for each language lives next to this file
# (RecoveryWithVerificationCodeOrMagicLinkMailBody[_<lang>].html).
# Loaded as UTF-8 via Properties.load(Reader). To add a new language, copy this file
# to RecoveryWithVerificationCodeOrMagicLinkMailMessages_<lang>.properties and translate
# the values only — keys must stay identical.

subject=Password recovery - Kadampa Booking System
