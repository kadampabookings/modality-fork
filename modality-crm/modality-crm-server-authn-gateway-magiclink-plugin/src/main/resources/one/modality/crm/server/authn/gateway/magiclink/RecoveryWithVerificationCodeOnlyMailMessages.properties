# English (default) dictionary for the verification-code-only password-recovery email.
# Out-of-body strings only — see RecoveryWithVerificationCodeOnlyMailBody[_<lang>].html
# for the matching HTML bodies. Same loading rules as the other *MailMessages files.

subject=Password recovery - Kadampa Booking System
