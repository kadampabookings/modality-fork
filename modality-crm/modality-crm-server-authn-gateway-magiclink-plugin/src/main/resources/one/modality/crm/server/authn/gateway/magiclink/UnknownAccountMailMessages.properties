# English (default) dictionary for the "unknown account" password-recovery email.
# Sent when the user requests recovery for an email that isn't registered.
# Out-of-body strings only — see UnknownAccountMailBody[_<lang>].html for the
# matching HTML bodies.

subject=Assistance with your Kadampa booking account
