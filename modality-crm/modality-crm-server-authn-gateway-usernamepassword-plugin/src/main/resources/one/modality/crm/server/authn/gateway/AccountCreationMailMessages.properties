# Default (English) dictionary for the account-creation magic-link email.
# Holds every piece of localized text that lives *outside* the HTML body
# (which has its own translated variant per language: AccountCreationMailBody_<lang>.html).
# Loaded as UTF-8 via Properties.load(Reader). To add a new language, copy
# this file to AccountCreationMailMessages_<lang>.properties and translate the
# values only — keys must stay identical.

subject=Account creation - Kadampa Booking System
