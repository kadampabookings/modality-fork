<?xml version="1.0" encoding="UTF-8"?>
<!-- File managed by WebFX (DO NOT EDIT MANUALLY) -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>one.modality</groupId>
        <artifactId>modality-base</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>modality-base-server-mailer-plugin</artifactId>

    <description>The Modality mailer: drains the shared mail table (out=true, untransmitted)
        and transmits each mail through the webfx mail transport, taking over the role of
        KBS2's legacy JavaMailSender. Behaviour-preserving port: same drain order (letter
        type ord), same 4s inter-send spacing, same sender resolution (awsSesVerified vs
        central address + Reply-To), same custom Message-ID, same bookkeeping (transmitted /
        error / recipient ok / document confirmed-cancelled-read side effects). The KBS2→KBS3
        handover is progressive: with drainScope = flagged (default) only letterless mails
        and letters flagged kbs3 are drained here, everything else stays with
        KBS2's mailer — both drain queries test the same DB column, so each mail has exactly
        one transmitter. The job is disabled by default (modality.base.server.mailer.enabled)
        and additionally requires a configured mail transport provider before it pulses.</description>

    <dependencies>

        <dependency>
            <groupId>one.modality</groupId>
            <artifactId>modality-base-shared-entities</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-async</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-boot</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-conf</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-console</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-meta</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-scheduler</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-util</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-db-datasource</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-mail-transport</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-mail-transport-sandbox-plugin</artifactId>
            <version>0.1.0-SNAPSHOT</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-mail-transport-smtp-vertx-plugin</artifactId>
            <version>0.1.0-SNAPSHOT</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-orm-datasourcemodel-service</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-orm-domainmodel</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-orm-entity</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-session-state</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

    </dependencies>

</project>