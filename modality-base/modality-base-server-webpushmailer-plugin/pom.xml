<?xml version="1.0" encoding="UTF-8"?>
<!-- File managed by WebFX (DO NOT EDIT MANUALLY) -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>one.modality</groupId>
        <artifactId>modality-base</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>modality-base-server-webpushmailer-plugin</artifactId>

    <description>The Modality web-push mailer: drains channel='push' mails (created by the
        system-letter trigger when a letter has a web-push variant and the booker a live
        subscription on its push context) and transmits each one as a Web Push notification
        to every live subscribed device of the booker, through webfx-stack-webpush-server.
        Bookkeeping mirrors the SMTP mailer (always marked transmitted — with the error
        recorded on failure — so a poison row can never block the queue), plus two
        push-specific duties: an endpoint answering 410/404 deletes the dead
        push_subscription row (recipient links cascade), and a mail with zero successful
        deliveries re-fires document.trigger_send_system_letter_id so the trigger composes
        the email variant instead (automatic email fallback — the already-created push row
        makes the trigger skip the push branch on the second pass). Disabled by default
        (modality.base.server.webpushmailer.enabled); sandbox mode processes the queue but
        logs instead of sending — the intended staging state, since a prod→staging DB
        refresh copies real device subscriptions.</description>

    <dependencies>

        <dependency>
            <groupId>one.modality</groupId>
            <artifactId>modality-base-shared-entities</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-async</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-boot</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-conf</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-console</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-meta</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-scheduler</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-platform-util</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-db-datasource</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-orm-datasourcemodel-service</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-orm-domainmodel</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-orm-entity</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-session-state</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>dev.webfx</groupId>
            <artifactId>webfx-stack-webpush-server</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

    </dependencies>

</project>