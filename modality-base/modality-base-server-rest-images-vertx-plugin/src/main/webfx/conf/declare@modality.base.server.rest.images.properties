# HMAC key of the Bunny pull zone's URL Token Authentication (Pull Zone -> Security).
# Per-zone value: staging and prod pull zones have different keys.
# Used to sign short-lived directory tokens for the protected /volunteers/ prefix.
pullZoneTokenKey = ${{ BUNNY_PULL_ZONE_TOKEN_KEY }}
